# 发布说明

## v3.0.0-local.12

### 🏷️ 程序更名
- 程序名称由「图转视频极速版 - 本地版」正式更名为「**图转视频极速版**」：窗口标题、安装包名称、应用内品牌展示同步更新
- 注意：安装目录随名称变化，旧版本安装请先卸载或覆盖到新目录

### 🩹 透明视频水印修复（关键）
- 修复：透明视频（MOV / WebM 带 alpha）作为视频水印时，OpenCV 解码丢弃透明通道导致**透明区域变成黑色方块**的问题
- 预览与导出均改为通过 FFmpeg 解码 RGBA 帧保留透明通道；正常混合走 FFmpeg 快速路径、高级混合走 OpenCV 路径都正确透明
- 现在「剪映导出透明视频 → 导入水印库 → 视频水印叠加」路线可真正使用

### ✨ 特效 / 转场演示图自定义
- 素材库 → 特效 / 转场 新增「演示图」设置：可自定义演示图 A（特效 / 转场首帧）与 B（转场第二帧），支持恢复默认
- 自定义后所有卡片预览立即刷新；设置持久化，临时目录清理后自动恢复

### 📥 从剪映导入
- 素材库 → BGM → 「从剪映导入」：自动扫描剪映草稿箱（支持手动选择目录）
- 解析新旧两种草稿格式，收集 BGM / 音效（导入 BGM 库，自动避重）与视频 / 图片（导入水印库）
- 剪映内置模板与曲库资源仅供个人本地使用

### 🩹 界面修复
- 素材库卡片模式操作按钮改为小图标单行排布，修复「加入图层」等文案在窄卡片下竖排的问题
- 按钮文字全局禁止折行（white-space: nowrap），同类窄容器（多选操作条、工具栏、弹窗底部等）支持整体换行

### ✅ 测试
- 引擎测试 97 个、前端类型检查 + 17 个单元测试 + 生产构建全部通过

---

## v3.0.0-local.11

### 🖼️ BGM 封面
- BGM 素材若内嵌专辑封面（MP3 ID3v2、FLAC / M4A 等常见格式），素材库自动显示对应封面
- 列表视图显示小封面，卡片视图显示大封面（含时长角标、播放中遮罩）；「从素材库选择 BGM」弹窗同步显示
- 封面提取在本地完成并缓存（按文件内容签名），无封面或提取失败时自动回退为音乐图标，不影响使用

### 🔔 添加素材状态提醒（覆盖全部素材类型）
- BGM / 水印：导入后提示「已导入 N 个素材 · 跳过重复 N 个 · 失败 N 个」
- BGM：批量拆解完成后弹出汇总提醒「成功 N · 重复跳过 N · 无音轨 N · 失败 N」（即使关闭素材库窗口也会收到）
- 特效 / 转场：加入 / 移出随机池即时提示
- 拆解源视频：拖拽添加时提示「已添加 N 个视频文件（已忽略非视频）」；非视频格式拖入给出明确错误提示

### 🛠️ 稳定性与测试
- 修复：封面提取临时文件后缀导致 ffmpeg 无法推断输出格式的问题（引擎侧）
- 新增封面提取测试（含缓存命中、无封面回退、缺失文件安全处理）；GUI 相关测试在无桌面会话环境自动跳过而非失败
- 引擎测试全套 90 个通过；前端类型检查 + 17 个单元测试 + 生产构建通过

---

## v3.0.0-local.10

### 🎵 素材库（重大新功能）
- 新增「素材库」：BGM / 水印 / 特效 / 转场 四类素材统一管理，支持文件夹树浏览、搜索、排序、多选与框选（圈选）批量操作
- BGM 与视频水印素材可直接从素材库选择使用（支持多选），不再只能通过目录方式指定；导出与预览均按所选素材生效
- 统一「从素材库选择素材」弹窗组件，所有入口（BGM、水印、特效、转场随机池）交互与样式一致
- 批量拆解 BGM：统一添加入口组件，支持点击选择文件、拖拽视频文件、选择文件夹三种方式
- 拆解结果自动避重：基于音频指纹检测重复素材，重复文件不会重复入库
- 素材支持新建文件夹、重命名、移动、删除；BGM / 水印素材库目录可切换

### 📥 更新机制优化（解决 GitHub 下载慢 / 失败）
- 更新检查自动按顺序尝试 GitHub 官方源与 3 个国内 ghproxy 镜像源，任一成功即用
- 更新弹窗新增：错误分类提示（网络 / 签名 / 格式分别说明）、镜像站加速下载、打开 GitHub 下载页、安装包大小显示；下载超时放宽至 10 分钟
- 可选「网络代理设置」：填写代理地址后，检查与下载均走代理
- 发布流程支持自动同步更新文件到 Gitee 镜像仓库（配置 `GITEE_TOKEN` / `GITEE_REPO` 后启用），国内用户可直连 Gitee 检查与下载更新

### 🎛️ 参数预设中心
- 新增参数预设中心：保存 / 应用 / 删除整套参数配置，一键切换常用预设

### 📋 日志抽屉
- 新增日志抽屉：实时查看引擎与任务日志，方便排查渲染 / 拆解问题

### ⚡ 渲染引擎与性能
- 引擎 sidecar 瘦身：约 167MB → 106MB（移除 PySide6 等无用依赖，改为惰性导入），发布包更小、启动更快
- 特效 / 转场素材面板：可见即加载静态预览帧，悬停加载动画帧（前端 LRU + 引擎磁盘缓存）
- 工作区列表操作（排序 / 复制 / 移动）重构为纯函数，并配套单元测试

### 🐛 问题修复
- 修复：从素材库选择 BGM 后，预览仍提示「请选择 BGM 目录」且没有声音 —— 预览逻辑现在优先使用素材库选定的文件
- 修复：素材库选定的 BGM 文件已不存在时，提示不准确（误报目录问题）
- 修复：选择素材后残留的校验提示不会自动消失
- 界面细节统一：主要操作按钮统一为蓝色底白字等

### ✅ 开发流程
- 新增代码检查工作流：提交 / PR 自动运行 Python 引擎测试（88 个）、前端类型检查与测试（17 个）、完整构建

---

## v3.0.0-local.9

- 桌面工作台发布：素材库初版（BGM / 水印管理、批量拆解 BGM）、统一素材选择弹窗
- 本地渲染引擎打包为 sidecar，随安装包分发

## v3.0.0-local.8

- 桌面工作台界面与引擎桥接稳定化，支持多任务并发渲染
