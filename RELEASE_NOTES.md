# 发布说明

## v3.0.0-local.23

### 🔑 修复自动更新：安装包签名密钥配错了
- **从 `3.0.0-local.18` 起，每版的自动更新都是坏的**：给安装包签名用的私钥，和程序内置的公钥
  不是同一对，点「检查更新」必定报 *「安装包签名校验失败：下载内容可能不完整或发布文件异常，请稍后重试。」*，
  重试无效。本次把公钥改回与签名私钥配对的那把，自动更新恢复正常
- ⚠️ **如果你现在装的是 `3.0.0-local.18` ~ `3.0.0-local.22` 中的任意一版，需要手动下载本版安装包安装一次**：
  旧版本内置的公钥已经写死在程序里，改不了。装上本版之后，往后的更新都能在应用内一键完成
- 新增 `tools/check_updater_key.py`：核对「配置的公钥 / 本机私钥 / 已发布产物的签名」三者是否配对，
  发布前后各跑一次，避免同一问题再发生

### ✅ 测试
- 校验脚本实测通过：配置公钥与已发布产物的签名 keyid 一致（`781FA49144971314`）
- Python 测试 188 项全部通过；前端类型检查与 52 项单元测试通过

---

## v3.0.0-local.22

### 📁 「按子文件夹抽取」预览与计数修复
- 此前切到该模式后预览会**静默不出图**：预览被「图片数」这一栏拦住，而该栏在此模式下本就置灰、
  后端也不校验它，界面上看不到任何提示。现在该模式的预览不再依赖「图片数」
- 预览取图与导出**完全对齐**：改为「每个子文件夹各取 1 张、按文件夹名顺序」，
  与成片的取图顺序一致；此前预览走全目录递归扫描，连隐藏目录里的图片都会排到最前面
- 检查器「图片数」下方新增**各子文件夹明细**：逐个列出文件夹名与图片张数，并给出合计与可组成的组合数；
  没有图片的空文件夹会单独提示已跳过
- 「换一组看看」在该模式下同样可用：只更换每个子文件夹里抽中的那一张，分组顺序与张数保持不变

### ⏱️ 填总时长即自动换算每张图片时长
- 填写「总时长」后，系统按 `总时长 ÷ 每个视频的图片数` **自动算出并填入「单图时长」**，不再需要手动设置
- 图片数、选图方式或目录内容变化时会**自动重算**；子文件夹模式下「图片数」即子文件夹个数
- 想手动指定单图时长，直接改「单图时长」即可——总时长会自动回到「自动」状态
- 修正整数倍校验：自动换算得到的时长（如 10 ÷ 3 = 3.3333）此前会被浮点舍入误差误判成「不是整数倍」

### 🧱 渲染内核与界面彻底解耦
- 渲染内核独立为 `src/render/` 共 15 个模块（转场 / 特效 / 合成 / 编码 / 水印 / 后处理 / 批量编排）
- 导出不再需要创建 Tk 窗口：新增无界面渲染 worker，命令与进度协议跟旧实现完全一致
- 界面侧移除约 4700 行耦合代码；打包引擎不再依赖 Tk / ImageTk
- 修复打包版引擎的渲染子进程仍指向旧界面桥接器的问题，与开发模式统一为同一条渲染路径

### ✅ 测试
- 真实渲染端到端验证：无界面 worker 与打包版同一入口（`--legacy-worker`）均成功产出 mp4
- Python 测试 182 项通过（另有 6 项为 Tk 界面遗留用例，与桌面版无关）；前端类型检查与 52 项单元测试通过

---

## v3.0.0-local.21

### 🎞️ 新增「按子文件夹抽取」：一次批量生成不重复的轮播
- 选图方式新增第三档：输入目录下按子文件夹（如 `1`、`2`、`3`、`4`、`5`）**各随机抽 1 张**，
  按文件夹名的自然顺序排成一轮，再循环播放到设定的总时长
- 生成多个视频时**优先使用完全不重复的组合**，每个组合最多出现一次；
  只有视频数超过组合总数时才允许重复，且重复被摊平——任意两个组合的出现次数最多相差 1 次
- 文件夹按目录名自然排序（`1 → 2 → 3 → 10`，而不是字符串序的 `1 → 10 → 2`）
- 该模式下每个视频的张数由子文件夹个数决定，界面上「图片数」已置灰，避免误以为改它有用

### 🎨 素材库跳转与布局修复
- 检查器的「从素材库选择」会**直接定位到对应标签页**（BGM / 水印 / 特效 / 转场），不再停在上次停留的位置
- 修复路径输入框在窄窗口下被右侧图标挤扁，以及参数区分组按钮换行错位的问题

### 🧹 内部清理
- 移除约 4300 行无引用的历史代码，不改变任何现有功能
- 发布流程新增「四处版本号一致性」校验，避免版本号漏改被打进发布包

### ✅ 测试
- Python 测试 188 项全部通过；前端类型检查与单元测试通过

---

## v3.0.0-local.20

### 💾 参数预设：默认保存输入/输出目录
- 保存预设时**默认一并保存输入/输出目录**，应用预设时自动恢复对应目录，跨项目一键还原整套配置
- 旧版预设（不含目录）应用时仍沿用当前工作区目录，不破坏已有使用习惯
- 同步修正更新签名密钥，修复旧版（3.0.0-local.18 及以上）自动更新时「安装包签名校验失败」的问题

### ✅ 测试
- 新增/更新预设提取与应用测试（含旧预设兼容）；前端类型检查 + 单元测试通过

---

## v3.0.0-local.19

### 🛡️ 素材库「体检清洗」：揪出假字幕轨 / 异常数据轨
- 部分后期/录制工具会在视频文件里写入一条**假时长轨**（字幕轨或 `encd` 数据轨），
  Windows 资源管理器与部分播放器会把它的时长当成视频时长显示——8 秒的视频可能显示成
  2 分多钟甚至 5 分多钟（视频本身时长是正确的）
- 素材库（BGM / 水印）工具栏新增「**体检清洗**」：
  - 一键扫描整个素材库，列出带字幕轨 / 异常数据轨 / 音频带视频轨的文件与原因
  - 勾选后一键清洗：**只去掉假轨、不重新编码**（画质不变、音质不变），
    原文件先移入系统回收站可还原
  - 支持清理 ffmpeg 也拦不住的**章节引用轨**（tref/chap）：直接重写 mp4 的 moov
    结构删除假轨并修正电影时长元数据，媒体数据一个字节都不动
- 新增命令行批量工具 `tools/clean_fake_tracks.py`：扫描任意目录，把带假轨的文件
  重封装为 `_clean` 后缀的干净版本（不动原文件），例如：
  `python tools/clean_fake_tracks.py "D:\视频" --dry-run` 先预览、再去掉 `--dry-run` 执行

### ✅ 测试
- 引擎新增体检/清洗测试 13 项（字幕轨、encd 数据轨、封面不误报、清洗后无残留等）；
  前端新增 3 项（扫描展示、一键清洗、全选/取消）；全套引擎 163 项 + 前端 39 项 +
  类型检查 + 生产构建全部通过

---

## v3.0.0-local.18

### ⏱️ 视频时长严格按设置生成
- **修复时长被撑长**：此前每图时长 < 0.5 秒时（如 0.1s/0.2s/0.3s），"每图至少 0.5 秒"的强制逻辑会把每张图撑到 0.5 秒，导致总时长远超设置（0.1s × 5 张曾生成 2.5s）。现已移除该钳制，输出时长严格等于 每图时长 × 图片数
- **消除帧数截断**：帧数计算由 `int()` 截断改为按总帧数四舍五入，余数由最后一张图吸收，实际时长与设置偏差不超过半帧
- **图片加载失败不再缩水**：缺图时用相邻帧补位保持总时长；首图失败才明确报错
- **BGM 不再截断视频**：非循环模式下用视频时长硬性限定输出（`-t`），BGM 比视频短也不会把视频裁短
- 新增 23 项时长回归测试，全部通过

### 🖼️ 图片水印：支持文件夹与随机贴图
- 水印图层"浏览"现在可**选择文件夹**（也可选单个文件），目录内全部图片作为水印池
- 未勾选"目录随机 1 个"时，每张图片从目录水印中**随机**贴一张（此前为顺序轮转）；勾选后整条视频固定随机一张
- 目录扫描支持 `.webp` / `.tiff` 水印

### 💾 参数预设完整性
- 预设现在包含**图片水印图层**（含路径与随机选项）、**视频水印路径**、**素材库 BGM 文件**，应用预设后水印与 BGM 配置完整恢复（此前被排除丢失）
- 新增预设保存/加载往返测试与 schema 覆盖测试

---

## v3.0.0-local.17

### 🏷️ 文件名前缀调整
- 日期前缀**只保留日期**（`YYYYMMDD`），不再附带时分秒：`20260816-video-1.mp4`（此前为 `20260816-153000-video-1.mp4`）
- 注意：同一天内多次生成时文件名可能相同并互相覆盖，如需区分请配合自定义前缀 / 首图名称 / 末尾序号使用

### 🗂️ 素材库：Eagle 风格素材管理
- **标签 / 星标 / 备注**：素材可打标签（自动去重、按数量汇总）、收藏标记、写文字备注；支持按标签筛选、只看收藏；素材名点击打开**详情面板**编辑（标签联想、星标、备注），元数据保存在素材库索引中，不修改源文件
- **查找重复素材**：扫描全库，水印库先按 SHA-256 精确分组、再按 dHash 感知哈希识别相似图片；BGM 库用响度指纹识别同曲不同版本；逐组勾选后一键移入回收站（每组默认保留一个）
- **批量重命名**：选中多个素材按模板重命名（`{n}` 序号自动补零、`{name}` 原文件名、`{date}` 当天日期），实时预览新旧文件名，冲突标红
- **缩略图尺寸调节**：卡片视图工具栏滑块调整缩略图大小（记忆上次设置）
- **智能文件夹**：按规则（类型 / 时长 / 大小 / 文件夹 / 名称 / 标签 / 收藏）自动归类的虚拟集合，实时筛选不移动文件；左侧栏一键查看、管理对话框可增删改
- 新增引擎接口：`library_set_metadata` / `library_get_tags` / `library_find_duplicates` / `library_rename_batch` / `library_smart_folders_list` / `library_smart_folders_save`
- 引擎测试 122 通过（新增元数据、去重、批量重命名、智能文件夹等 9 个）；前端 36 个单元测试 + 类型检查 + 生产构建全部通过

---

## v3.0.0-local.16

### 🗂️ 素材库：安全删除与效率
- 删除素材/文件夹**先确认**，且改走**系统回收站**（新增 `Send2Trash` 依赖），可还原；删除失败不再静默
- 新增**批量删除**（一次调用处理多文件，不再逐个串行 IPC）
- 新增**素材重命名**（✎ 弹窗，强制保留原扩展名）
- 新增**拖拽移动**：「移动」按钮即拖拽手柄，拖到左侧文件夹树直接移动，目标高亮
- 删除/重命名后**本地增量更新**（含文件夹计数重算），不再全量重扫；打开素材库复用快照（仅首次扫描）

### 🔍 搜索与筛选
- 搜索改为匹配「文件名 + 所在文件夹」；水印库新增**图片/视频 类型筛选**；搜索输入 150ms 防抖
- 入库时间人性化显示（不再显示原始 ISO 时间戳）
- 水印缩略图按路径缓存，切换视图/搜索不再重复请求引擎

### 🎬 播放预览
- 视频水印预览**横竖屏自适应**（竖屏显示竖板，不再固定横板黑边）
- 图片水印新增**放大预览**（棋盘格背景突出透明 PNG）
- 新增**BGM 试听进度条**（播放行显示进度与时间，点击可定位）
- **性能优化**：音频预览对可播放格式（mp3/wav/m4a/aac/ogg/flac）直接拷贝、不再强制转码；去掉每次调用的无用 ffprobe；进度条改为自包含组件——**首次播放延迟与播放中卡顿大幅降低**

### 🩹 交互修复
- 修复点击视频/图片预览空白处会冒泡关闭整个素材库、跳回主界面的问题（现只关闭当前弹窗）
- 导入素材新增**导入中状态提醒**（按钮转圈 + 「正在导入 N 项…」+ 开始/完成提示）
- 更换目录时说明「原目录文件不会被移动或删除」；删除非空文件夹给出友好提示
- 空库时内嵌「导入/批量拆BGM」引导按钮；素材库内支持 Ctrl+A 全选

### ✅ 测试
- 引擎测试 110 通过（1 个 Tk GUI 测试因环境跳过）；前端 32 个单元测试 + 类型检查 + 生产构建全部通过

---

## v3.0.0-local.15

### 🩹 修复：从剪映导入内置资源的按钮无反应
- 修复「从剪映导入 → 内置资源（缓存）」中**「导入 BGM」「导入水印库」按钮点击无反应**：按钮回调持有旧的扫描结果闭包（`useCallback` 依赖缺失），扫描完成后点击静默无效；现已补全依赖，点击即真正开始导入
- 导入操作新增**操作反馈（不增加操作步骤）**：
  - 点击后对话框内出现「正在导入 BGM / 音效…（自动避重，请稍候）」进行中提示，按钮同步转圈禁用
  - 完成后对话框内保留结果汇总（绿色成功：「已从剪映导入 X 条 BGM，跳过重复 Y 条」；红色失败原因），右上角通知不变
  - 未扫描完成 / 无可导入素材时给出明确提示，不再静默无反应
  - 重新扫描、切换来源或重开对话框时自动清除旧结果，避免误读
- 草稿素材页同样补全反馈，行为一致

### ✅ 测试
- 新增前端回归测试 4 个（`MaterialLibrary`：缓存 BGM / 水印导入、草稿汇总导入），并验证测试可复现原缺陷
- 引擎测试 107 个 + 前端 21 个单元测试 + 类型检查 + 生产构建全部通过

---

## v3.0.0-local.14

### 📥 从剪映导入：内置资源 + 特效/转场
- 「从剪映导入」新增**来源切换**：草稿素材（原有）/ **内置资源（缓存）**（新增）
- 内置资源：直接扫描剪映 `User Data\Cache`，导入已下载的内置 BGM / 音效（自动避重）与特效 / 转场媒体资源（导入水印库，透明视频保留 alpha 作视频水印叠加）；资源名称尽力从清单 JSON 解析
- 草稿扫描扩展：`materials.effects` / `materials.transitions` 的本地资源型条目也会收集（纯云端模板自动跳过）
- 仅个人本地使用；未下载的云端资源需先在剪映中使用/下载

### 🎛️ 随机池配置迁移到素材库
- 检查器不再弹「配置随机池」窗口，改为「在素材库配置」按钮，一键打开素材库并定位到特效 / 转场标签
- 素材库新增**随机池管理条**：池内成员胶囊列表（逐个 ✕ 移除）+ 一键清空，配置随机池统一在素材库完成

### 🩹 界面跳动修复（操作体验）
- 修复弹窗开合时整体横跳（body 滚动条槽位常驻）
- 修复更新 / 拆 BGM / 视频播放器弹窗高度随内容变化导致的上下跳动（固定弹窗高度）
- 滚动容器统一保留滚动条槽位（素材列表、效果网格、日志等），内容不再横向位移
- 校验提醒改为**顶部悬浮小弹窗**（不占布局、可手动收起、提醒内容变化后重新出现），不再挤压主界面
- 动态文字按钮固定最小宽度，消除相邻控件位移

### ✅ 测试
- 引擎测试 107 个（新增剪映缓存扫描、特效/转场资源收集等）、前端类型检查 + 17 个单元测试 + 生产构建全部通过

---

## v3.0.0-local.13

### 🎬 素材库视频水印放大播放预览
- 水印库视频素材新增**放大播放器**：点击缩略图（悬停时显示 ▶ 提示）或列表 ▶ 按钮，弹出大屏播放（自动播放 + 原生控制条 + 全屏）
- 引擎自动把视频准备为可播放格式：h264 mp4/mov、webm 直接拷贝；avi/mkv/ProRes 透明 MOV 等自动转码为 H.264
- 点背景 / ✕ / Esc 关闭；BGM 试听与视频预览互斥

### 🏷️ 文件名前缀优化
- 文件名所有连接符统一为 `-`（含日期前缀内部）：`20260816-153000-video-1.mp4`
- 末尾自动序号不再补零：`video-1.mp4`、`video-10.mp4`、`video-123.mp4`
- 自定义前缀留空时不再自动注入 "video" 等无关文案：`20260816-153000-1.mp4`

### 🩹 播放按钮图标纠正
- 「用作视频水印」按钮不再使用 ▶ 播放图标（避免误认为播放键），改为印章图标；播放入口统一为缩略图悬停 ▶ / 列表 ▶ 按钮

### ✅ 测试
- 引擎测试 104 个（新增视频预览直拷/转码/缓存、文件名命名规则等）、前端类型检查 + 17 个单元测试 + 生产构建全部通过

---

## v3.0.0-local.12

### 🏷️ 程序更名
- 程序名称由「图转视频极速版 - 本地版」正式更名为「**图转视频极速版**」：窗口标题、安装包名称、应用内品牌展示同步更新
- 注意：安装目录随名称变化，旧版本安装请先卸载或覆盖到新目录

### 🩹 透明视频水印修复（关键）
- 修复：透明视频（MOV / WebM 带 alpha）作为视频水印时，OpenCV 解码丢弃透明通道导致**透明区域变成黑色方块**的问题
- 预览与导出均改为通过 FFmpeg 解码 RGBA 帧保留透明通道；正常混合走 FFmpeg 快速路径、高级混合走 OpenCV 路径都正确透明
- 现在「剪映导出透明视频 → 导入水印库 → 视频水印叠加」路线可真正使用

### ✨ 特效 / 转场演示图自定义
- 素材库 → 特效 / 转场 新增「演示图」设置：可自定义演示图 A（特效 / 转场首帧）与 B（转场第二帧），支持恢复默认
- 自定义后所有卡片预览立即刷新；设置持久化，临时目录清理后自动恢复

### 📥 从剪映导入
- 素材库 → BGM → 「从剪映导入」：自动扫描剪映草稿箱（支持手动选择目录）
- 解析新旧两种草稿格式，收集 BGM / 音效（导入 BGM 库，自动避重）与视频 / 图片（导入水印库）
- 剪映内置模板与曲库资源仅供个人本地使用

### 🩹 界面修复
- 素材库卡片模式操作按钮改为小图标单行排布，修复「加入图层」等文案在窄卡片下竖排的问题
- 按钮文字全局禁止折行（white-space: nowrap），同类窄容器（多选操作条、工具栏、弹窗底部等）支持整体换行

### ✅ 测试
- 引擎测试 97 个、前端类型检查 + 17 个单元测试 + 生产构建全部通过

---

## v3.0.0-local.11

### 🖼️ BGM 封面
- BGM 素材若内嵌专辑封面（MP3 ID3v2、FLAC / M4A 等常见格式），素材库自动显示对应封面
- 列表视图显示小封面，卡片视图显示大封面（含时长角标、播放中遮罩）；「从素材库选择 BGM」弹窗同步显示
- 封面提取在本地完成并缓存（按文件内容签名），无封面或提取失败时自动回退为音乐图标，不影响使用

### 🔔 添加素材状态提醒（覆盖全部素材类型）
- BGM / 水印：导入后提示「已导入 N 个素材 · 跳过重复 N 个 · 失败 N 个」
- BGM：批量拆解完成后弹出汇总提醒「成功 N · 重复跳过 N · 无音轨 N · 失败 N」（即使关闭素材库窗口也会收到）
- 特效 / 转场：加入 / 移出随机池即时提示
- 拆解源视频：拖拽添加时提示「已添加 N 个视频文件（已忽略非视频）」；非视频格式拖入给出明确错误提示

### 🛠️ 稳定性与测试
- 修复：封面提取临时文件后缀导致 ffmpeg 无法推断输出格式的问题（引擎侧）
- 新增封面提取测试（含缓存命中、无封面回退、缺失文件安全处理）；GUI 相关测试在无桌面会话环境自动跳过而非失败
- 引擎测试全套 90 个通过；前端类型检查 + 17 个单元测试 + 生产构建通过

---

## v3.0.0-local.10

### 🎵 素材库（重大新功能）
- 新增「素材库」：BGM / 水印 / 特效 / 转场 四类素材统一管理，支持文件夹树浏览、搜索、排序、多选与框选（圈选）批量操作
- BGM 与视频水印素材可直接从素材库选择使用（支持多选），不再只能通过目录方式指定；导出与预览均按所选素材生效
- 统一「从素材库选择素材」弹窗组件，所有入口（BGM、水印、特效、转场随机池）交互与样式一致
- 批量拆解 BGM：统一添加入口组件，支持点击选择文件、拖拽视频文件、选择文件夹三种方式
- 拆解结果自动避重：基于音频指纹检测重复素材，重复文件不会重复入库
- 素材支持新建文件夹、重命名、移动、删除；BGM / 水印素材库目录可切换

### 📥 更新机制优化（解决 GitHub 下载慢 / 失败）
- 更新检查自动按顺序尝试 GitHub 官方源与 3 个国内 ghproxy 镜像源，任一成功即用
- 更新弹窗新增：错误分类提示（网络 / 签名 / 格式分别说明）、镜像站加速下载、打开 GitHub 下载页、安装包大小显示；下载超时放宽至 10 分钟
- 可选「网络代理设置」：填写代理地址后，检查与下载均走代理
- 发布流程支持自动同步更新文件到 Gitee 镜像仓库（配置 `GITEE_TOKEN` / `GITEE_REPO` 后启用），国内用户可直连 Gitee 检查与下载更新

### 🎛️ 参数预设中心
- 新增参数预设中心：保存 / 应用 / 删除整套参数配置，一键切换常用预设

### 📋 日志抽屉
- 新增日志抽屉：实时查看引擎与任务日志，方便排查渲染 / 拆解问题

### ⚡ 渲染引擎与性能
- 引擎 sidecar 瘦身：约 167MB → 106MB（移除 PySide6 等无用依赖，改为惰性导入），发布包更小、启动更快
- 特效 / 转场素材面板：可见即加载静态预览帧，悬停加载动画帧（前端 LRU + 引擎磁盘缓存）
- 工作区列表操作（排序 / 复制 / 移动）重构为纯函数，并配套单元测试

### 🐛 问题修复
- 修复：从素材库选择 BGM 后，预览仍提示「请选择 BGM 目录」且没有声音 —— 预览逻辑现在优先使用素材库选定的文件
- 修复：素材库选定的 BGM 文件已不存在时，提示不准确（误报目录问题）
- 修复：选择素材后残留的校验提示不会自动消失
- 界面细节统一：主要操作按钮统一为蓝色底白字等

### ✅ 开发流程
- 新增代码检查工作流：提交 / PR 自动运行 Python 引擎测试（88 个）、前端类型检查与测试（17 个）、完整构建

---

## v3.0.0-local.9

- 桌面工作台发布：素材库初版（BGM / 水印管理、批量拆解 BGM）、统一素材选择弹窗
- 本地渲染引擎打包为 sidecar，随安装包分发

## v3.0.0-local.8

- 桌面工作台界面与引擎桥接稳定化，支持多任务并发渲染
