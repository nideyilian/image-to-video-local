# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户画像尚未明确确认。当前功能证据指向需要在 Windows 本地批量将图片制作成视频的内容创作者或运营人员；后续研究需要确认个人创作者与团队批量生产场景的优先级。

## Product Purpose

在用户自己的 Windows 电脑上，将一批图片快速生成一个或多个视频。成功意味着用户能够配置画面、转场、动态效果、音频与水印，预览结果，并可靠地完成批量导出。

## Positioning

所有素材扫描、预览与视频渲染都在本机完成；产品把多视频批量任务、丰富效果、BGM 和多层水印放在同一条离线工作流中。

## Operating Context

- Windows 10/11 本地桌面环境。
- 输入为本地图片目录，可附加本地 BGM、视频水印和多层图片水印。
- 输出依赖 Python、OpenCV、MoviePy 与 FFmpeg。
- 用户需要在长时间渲染期间查看阶段、进度与速度，并能够暂停、继续或取消。
- 多个配置工作区可以并行排队并按设备能力限制并发。

## Capabilities and Constraints

- 必须保留现有图片选择、输出数量、时长、FPS、分辨率、格式、编码和码率配置。
- 必须保留单一或随机转场、单图动态效果、实时预览与时间轴。
- 必须保留 BGM、视频水印、多层图片水印、混合方式和音频策略。
- 必须保留配置保存/恢复、多工作区、批量处理、暂停、取消、进度、性能统计和内存优化。
- 产品是本地版，不包含远程渲染、HTTP 服务、FRP 或剪映素材工作流。
- 新界面使用 Tauri 2 与 React/TypeScript；现有 Python 处理能力通过独立无界面引擎保留。
- 迁移期间旧 Qt/Tk 界面必须保持可运行，直到新界面达到功能对等。
- 当前配置 JSON 是兼容边界；迁移不得静默改变已有配置含义。

## Brand Commitments

- 保留“图转视频极速版 - 本地版”的产品名称，除非用户后续决定更名。
- 界面与文案使用简体中文。
- 产品必须明确表达本地处理和任务状态，不虚构云端能力。

## Evidence on Hand

- `README.md` 描述本地版边界、环境与功能。
- `src/gui_qt/main_window.py` 是当前 PySide6 界面及预览、任务调度实现。
- `src/gui/main_window.py` 包含旧 Tk 界面和当前生产处理管线。
- `config/default_settings.json`、运行时配置文件与现有测试是兼容性证据。
- 没有现成品牌规范、用户研究、商业数据或设计素材；后续界面不得虚构这些内容。

## Product Principles

1. 任务与状态优先：重要操作、进度、错误和恢复路径始终清晰。
2. 本地且可控：素材不离开电脑，长任务可以暂停、取消和追踪。
3. 复杂能力渐进呈现：常用配置快速可达，高级效果按需展开。
4. 迁移保持结果一致：新界面不能改变既有处理语义或配置含义。
5. 批量生产仍要可理解：每个工作区和任务都能独立识别、检查和恢复。

## Accessibility & Inclusion

新界面需要支持键盘操作、可见焦点、足够的颜色对比度、非颜色状态提示，以及 Windows 常见显示缩放比例。
